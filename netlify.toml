[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for client-side routes (e.g. /dashboard, /workflow)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
